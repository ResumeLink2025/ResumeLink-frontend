'use client';

import { PageWrapper } from '@/layouts';

import LoginSection from './LoginSection';

const LoginPage = () => {
  return (
    <PageWrapper>
      <LoginSection />
    </PageWrapper>
  );
};

export default LoginPage;
