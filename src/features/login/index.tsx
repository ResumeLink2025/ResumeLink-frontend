'use client';

import { PageWrapper } from '@/layouts';

import LoginSection from './LoginSection';

const Login = () => {
  return (
    <PageWrapper>
      <LoginSection />
    </PageWrapper>
  );
};

export default Login;
