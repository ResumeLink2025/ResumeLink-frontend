'use client';

import { PageWrapper } from '@/layouts';

import LoginSection from './LoginSection';

const Landing = () => {
  return (
    <PageWrapper>
      <LoginSection />
    </PageWrapper>
  );
};

export default Landing;
