'use client';

import { PageWrapper } from '@/layouts';

import LoginSection from './LoginSection';

const index = () => {
  return (
    <PageWrapper>
      <LoginSection />
    </PageWrapper>
  );
};

export default index;
