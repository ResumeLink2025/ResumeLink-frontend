'use client';

import { PageWrapper } from '@/layouts';

import RegisterSection from './registerSection';

const Landing = () => {
  return (
    <PageWrapper>
      <RegisterSection />
    </PageWrapper>
  );
};

export default Landing;
